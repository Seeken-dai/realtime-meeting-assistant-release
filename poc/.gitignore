config.py
__pycache__/
*.pyc
.venv/
venv/
*.wav
*.pcm

# 知识库文档默认不跟踪：这里是命令行调试的回退目录，
# 一旦丢进真实客户资料，git add -A 会顺手提交上去。
# 例外是下面三份虚构示例——run_suggest.py 的 4 个预设场景依赖它们，
# 不保留的话新克隆跑不起来。
# ⚠️ 新增文档一律不跟踪；确需入库的示例请显式加白名单。
docs/*
!docs/产品功能清单.md
!docs/历史项目案例.md
!docs/需求边界与报价规则.md
